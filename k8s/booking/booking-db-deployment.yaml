apiVersion: apps/v1
kind: Deployment

metadata:
  name: booking-db

spec:
  replicas: 1

  selector:
    matchLabels:
      app: booking-db

  template:
    metadata:
      labels:
        app: booking-db

    spec:
      containers:
        - name: postgres
          image: postgres:17

          env:
            - name: POSTGRES_DB
              value: seatnow_bookings

            - name: POSTGRES_USER
              value: postgres

            - name: POSTGRES_PASSWORD
              value: postgres

          ports:
            - containerPort: 5432

          volumeMounts:
            - name: booking-db-storage
              mountPath: /var/lib/postgresql/data

      volumes:
        - name: booking-db-storage
          persistentVolumeClaim:
            claimName: booking-db-data